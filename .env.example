# OpenAI Configuration
# IMPORTANT: This key is only used server-side. Never expose it in client code.
OPENAI_API_KEY=sk-your-openai-api-key-here

# Database Configuration (optional - will use SQLite by default if not specified)
# DATABASE_URL=postgresql://user:password@localhost:5432/keyframes_action

# Server Configuration (optional)
# PORT=5000
# NODE_ENV=development

# Security Configuration (add these for production)
# JWT_SECRET=your-secret-jwt-key-here
# JWT_REFRESH_SECRET=your-refresh-jwt-key-here
# SESSION_SECRET=your-session-secret-here

# CORS Configuration
# In development, all localhost origins are allowed by default
# In production, specify allowed origins:
# ALLOWED_ORIGINS=https://keyframes.getmoreviews.com,https://www.keyframes.getmoreviews.com
# For strict CORS on sensitive endpoints:
# STRICT_ORIGINS=https://admin.keyframes.getmoreviews.com

# Rate Limiting (optional)
# RATE_LIMIT_WINDOW_MS=900000  # 15 minutes
# RATE_LIMIT_MAX_REQUESTS=100  # requests per window